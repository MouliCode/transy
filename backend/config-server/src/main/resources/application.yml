server:
  port: ${CONFIG_PORT:8888}

spring:
  application:
    name: config-server

app:
  security:
    ws-api-key: ${WS_API_KEY:dev-ws-key}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
