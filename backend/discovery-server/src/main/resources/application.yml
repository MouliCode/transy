server:
  port: ${DISCOVERY_PORT:8761}

spring:
  application:
    name: discovery-server

app:
  security:
    ws-api-key: ${WS_API_KEY:dev-ws-key}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
