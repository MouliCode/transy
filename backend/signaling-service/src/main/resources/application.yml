server:
  port: ${SIGNALING_PORT:8082}

spring:
  application:
    name: signaling-service

app:
  security:
    ws-api-key: ${WS_API_KEY:dev-ws-key}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
