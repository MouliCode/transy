server:
  port: ${TRANSFER_PORT:8085}

spring:
  application:
    name: transfer-service

app:
  security:
    ws-api-key: ${WS_API_KEY:dev-ws-key}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
