server:
  port: ${GATEWAY_PORT:8080}

spring:
  application:
    name: api-gateway

app:
  security:
    ws-api-key: ${WS_API_KEY:dev-ws-key}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
